import React from 'react'
import Card from './card';


const app = (props)=>{
    return (
		<div>
			<Card quote="Real Learners start From 0" />
			<Card quote="The only reward for your success is a next challenge" />
			<Card quote="The struggle is the way" />
		</div>
	);
}

export default app